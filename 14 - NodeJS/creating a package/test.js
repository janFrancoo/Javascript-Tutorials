console.log("This is JanFranco");

// Installed packages -> dependencies
// npm install -> find package.json and install packages in dependencies
// npm run janfranco -> find package.json scripts then run command in scripts['janfranco']
// for installing a package to a git system, add dependencies {type, url} in package.json
// for installing a package to npmjs.com => npm login, npm publish (for remove npm unpublish package-name --force)
